The Alpine version of the same image is significantly smaller (83mb vs 15.7mb).

The secret message functionality seems to be the same, but execing inside the container needs to be done with sh shell, not bash.
