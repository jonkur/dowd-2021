1. Start with 'docker compose up --build'
2. Frontend is running at localhost:80
3. You can check the user status by execing into the running frontend or backend containers, whoami should show the current user as "nonrootuser" for both.
